// Secrets.swift.template — copy to Secrets.swift and fill in real values.
// One token per backend (same tokens the single widgets use).

enum Secrets {
  static let pigBase = "https://pig.maytek.co"
  static let pigToken = "change-me"

  static let shrimpBase = "https://shrimp.maytek.co"
  static let shrimpToken = "change-me"

  static let grainsBase = "https://grains.maytek.co"
  static let grainsToken = "change-me"
}
